class {{ name }}(str{% if values %}, Enum{% endif %}):
{% if description %}
    """
    {{ description }}
    """
{% endif %}
{% if values %}
    {% for pv in values.values() -%}
        {% if pv.description %}
    # {{pv.description}}
        {% endif %}
    {{pv.label}}{{"_" if (pv.label | is_keyword)}} = "{{pv.value}}"
    {% endfor %}
{% else %}
    pass
{% endif %}